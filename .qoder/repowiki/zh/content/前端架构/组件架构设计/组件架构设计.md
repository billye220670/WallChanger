# 组件架构设计

<cite>
**本文档引用的文件**
- [src\App.tsx](file://src\App.tsx)
- [src\main.tsx](file://src\main.tsx)
- [src\store.ts](file://src\store.ts)
- [src\types.ts](file://src\types.ts)
- [src\screens\UploadScreen.tsx](file://src\screens\UploadScreen.tsx)
- [src\screens\ProcessingScreen.tsx](file://src\screens\ProcessingScreen.tsx)
- [src\screens\EditorScreen.tsx](file://src\screens\EditorScreen.tsx)
- [src\screens\FinalizingScreen.tsx](file://src\screens\FinalizingScreen.tsx)
- [src\screens\ResultSheet.tsx](file://src\screens\ResultSheet.tsx)
- [src\components\ImageCanvas.tsx](file://src\components\ImageCanvas.tsx)
- [src\components\MaterialDrawer.tsx](file://src\components\MaterialDrawer.tsx)
- [src\utils\api.ts](file://src\utils\api.ts)
- [src\utils\canvas.ts](file://src\utils\canvas.ts)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

WallChanger 是一个基于 React 的室内材质替换应用，采用函数组件和 Hooks 设计模式，通过 Zustand 状态管理实现清晰的组件职责分离。该应用实现了从图像上传、AI 自动分割、材质拖拽替换到最终渲染输出的完整工作流。

## 项目结构

项目采用按功能模块组织的目录结构，主要分为以下层次：

```mermaid
graph TB
subgraph "应用入口层"
main_tsx[main.tsx]
app_tsx[App.tsx]
end
subgraph "状态管理层"
store_ts[store.ts]
types_ts[types.ts]
end
subgraph "屏幕组件层"
upload[UploadScreen]
processing[ProcessingScreen]
editor[EditorScreen]
finalizing[FinalizingScreen]
result[ResultSheet]
end
subgraph "可复用UI组件层"
image_canvas[ImageCanvas]
material_drawer[MaterialDrawer]
drag_preview[DragPreview]
debug_panel[DebugPanel]
examples_drawer[ExamplesDrawer]
settings_drawer[SettingsDrawer]
material_tile[MaterialTile]
region_highlight[RegionHighlight]
end
subgraph "工具层"
api_utils[utils/api.ts]
canvas_utils[utils/canvas.ts]
coords_utils[utils/coords.ts]
remap_utils[utils/remapMaskColors.ts]
end
main_tsx --> app_tsx
app_tsx --> store_ts
app_tsx --> upload
app_tsx --> processing
app_tsx --> editor
app_tsx --> finalizing
app_tsx --> result
upload --> store_ts
processing --> store_ts
editor --> store_ts
finalizing --> store_ts
result --> store_ts
editor --> image_canvas
editor --> material_drawer
material_drawer --> api_utils
image_canvas --> canvas_utils
editor --> canvas_utils
api_utils --> types_ts
canvas_utils --> types_ts
```

**图表来源**
- [src\main.tsx:1-11](file://src\main.tsx#L1-L11)
- [src\App.tsx:1-26](file://src\App.tsx#L1-L26)
- [src\store.ts:1-177](file://src\store.ts#L1-L177)

**章节来源**
- [src\main.tsx:1-11](file://src\main.tsx#L1-L11)
- [src\App.tsx:1-26](file://src\App.tsx#L1-L26)

## 核心组件

### 应用主组件 App.tsx

App.tsx 作为整个应用的根组件，采用条件渲染模式根据应用状态切换不同的屏幕组件：

```mermaid
flowchart TD
Start([应用启动]) --> GetPhase[获取当前阶段状态]
GetPhase --> CheckUpload{阶段是否为 upload?}
CheckUpload --> |是| ShowUpload[显示 UploadScreen]
CheckUpload --> |否| CheckProcessing{阶段是否为 processing?}
CheckProcessing --> |是| ShowProcessing[显示 ProcessingScreen]
CheckProcessing --> |否| CheckEditing{阶段是否为 editing?}
CheckEditing --> |是| ShowEditor[显示 EditorScreen]
CheckEditing --> |否| CheckFinalizing{阶段是否为 finalizing?}
CheckFinalizing --> |是| ShowFinalizing[显示 FinalizingScreen]
CheckFinalizing --> |否| CheckDone{阶段是否为 done?}
CheckDone --> |是| ShowBoth[同时显示 EditorScreen 和 ResultSheet]
CheckDone --> |否| ShowUpload
ShowUpload --> End([渲染完成])
ShowProcessing --> End
ShowEditor --> End
ShowFinalizing --> End
ShowBoth --> End
```

**图表来源**
- [src\App.tsx:8-25](file://src\App.tsx#L8-L25)

### 状态管理 Store.ts

使用 Zustand 实现集中式状态管理，包含应用的所有状态和操作方法：

```mermaid
classDiagram
class AppStore {
+Phase phase
+string originalImage
+Dimensions dimensions
+string refinedMask
+string rawMask
+string[] maskImages
+MaskInfo[] masks
+string compositeImage
+string finalImage
+number processingStep
+Set~number~ processingRegions
+Map~number,string~ appliedRegions
+boolean isApplying
+Material draggingMaterial
+number hoveredMaskId
+boolean batchMode
+BatchItem[] batchItems
+string backendUrl
+DebugPrompts debugPrompts
+boolean debugMode
+setPhase(phase)
+setOriginalImage(image, width, height)
+setMasks(enforcedResult, maskImages, masks)
+setMaskImages(maskImages, masks)
+setProcessingStep(step)
+addProcessingRegion(maskId)
+removeProcessingRegion(maskId)
+setAppliedRegion(maskId, image)
+setCompositeImage(image)
+setFinalImage(image)
+setDraggingMaterial(material)
+setHoveredMaskId(id)
+setBackendUrl(url)
+setDebugPrompts(prompts)
+setDebugMode(enabled)
+setIsApplying(v)
+setBatchMode(enabled)
+addBatchItem(item)
+removeBatchItem(id)
+clearBatchItems()
+loadExample(...)
+reset()
}
class AppState {
+Phase phase
+Dimensions dimensions
+string[] maskImages
+MaskInfo[] masks
+boolean batchMode
+BatchItem[] batchItems
+string backendUrl
+DebugPrompts debugPrompts
+boolean debugMode
}
AppStore --|> AppState : "扩展"
```

**图表来源**
- [src\store.ts:5-28](file://src\store.ts#L5-L28)
- [src\types.ts:57-88](file://src\types.ts#L57-L88)

**章节来源**
- [src\store.ts:1-177](file://src\store.ts#L1-L177)
- [src\types.ts:1-89](file://src\types.ts#L1-L89)

## 架构总览

系统采用分层架构设计，各层职责明确：

```mermaid
graph TB
subgraph "表现层"
UI[React 组件树]
Screens[屏幕组件]
Components[可复用UI组件]
end
subgraph "状态管理层"
Zustand[Zustand Store]
State[应用状态]
end
subgraph "业务逻辑层"
API[API服务调用]
Canvas[画布渲染]
Utils[工具函数]
end
subgraph "数据层"
Backend[后端服务]
LocalStorage[本地存储]
end
UI --> Zustand
Screens --> Zustand
Components --> Zustand
Zustand --> State
Zustand --> API
Zustand --> Canvas
API --> Backend
Canvas --> Utils
State --> LocalStorage
```

**图表来源**
- [src\App.tsx:1-26](file://src\App.tsx#L1-L26)
- [src\store.ts:63-176](file://src\store.ts#L63-L176)

## 详细组件分析

### UploadScreen 上传屏幕

UploadScreen 负责处理用户图像上传和初始配置：

```mermaid
sequenceDiagram
participant User as 用户
participant Upload as UploadScreen
participant Store as Zustand Store
participant API as 后端API
User->>Upload : 选择或拖拽图片
Upload->>Upload : 验证文件类型
Upload->>Upload : 读取图片尺寸
Upload->>Store : setOriginalImage(base64, width, height)
Upload->>Store : setPhase('processing')
Upload->>API : 触发预处理流程
API-->>Upload : 返回处理结果
Upload->>Upload : 跳转到编辑界面
```

**图表来源**
- [src\screens\UploadScreen.tsx:13-29](file://src\screens\UploadScreen.tsx#L13-L29)
- [src\screens\UploadScreen.tsx:6-7](file://src\screens\UploadScreen.tsx#L6-L7)

**章节来源**
- [src\screens\UploadScreen.tsx:1-121](file://src\screens\UploadScreen.tsx#L1-L121)

### ProcessingScreen 处理屏幕

ProcessingScreen 实现 AI 图像处理和蒙版生成：

```mermaid
flowchart TD
Start([进入处理阶段]) --> InitEffect[初始化副作用]
InitEffect --> CheckImage{是否有原始图像?}
CheckImage --> |否| Wait[等待输入]
CheckImage --> |是| SetBackend[设置后端URL]
SetBackend --> CallAPI[调用预处理API]
CallAPI --> ValidateResponse{验证响应格式}
ValidateResponse --> |失败| ShowError[显示错误信息]
ValidateResponse --> |成功| BuildMasks[构建蒙版信息]
BuildMasks --> SetMasks[更新蒙版状态]
SetMasks --> Transition[跳转到编辑阶段]
ShowError --> Wait
Transition --> End([处理完成])
Wait --> End
```

**图表来源**
- [src\screens\ProcessingScreen.tsx:34-90](file://src\screens\ProcessingScreen.tsx#L34-L90)

**章节来源**
- [src\screens\ProcessingScreen.tsx:1-135](file://src\screens\ProcessingScreen.tsx#L1-L135)

### EditorScreen 编辑屏幕

EditorScreen 是最复杂的屏幕组件，实现材质拖拽、区域分割和实时渲染：

```mermaid
classDiagram
class EditorScreen {
+useState selectedMaskId
+useState editing
+useState lineStart
+useState lineEnd
+useRef canvasRef
+useRef overlayCanvasRef
+useRef processingCanvasRef
+handleDragStart(material, x, y)
+handleDragMove(x, y)
+handleDragEnd(x, y)
+handleImageClick(e)
+handleApply()
+handleLinePointerDown(e)
+handleLinePointerMove(e)
+handleLinePointerUp(e)
+drawMaskOutline()
+drawMaskShimmer()
+drawProcessingShimmer()
+drawMaskDim()
}
class ImageCanvas {
+forwardRef getCanvas()
+forwardRef exportBase64()
+useEffect 初始化画布
+useEffect 更新复合图像
}
class MaterialDrawer {
+useState materials
+useState loading
+handlePointerDown(e)
+handlePointerMove(e)
+handlePointerUp(e)
}
EditorScreen --> ImageCanvas : "组合"
EditorScreen --> MaterialDrawer : "组合"
EditorScreen --> CanvasUtils : "使用"
EditorScreen --> APIUtils : "使用"
```

**图表来源**
- [src\screens\EditorScreen.tsx:21-51](file://src\screens\EditorScreen.tsx#L21-L51)
- [src\components\ImageCanvas.tsx:15-31](file://src\components\ImageCanvas.tsx#L15-L31)
- [src\components\MaterialDrawer.tsx:15-13](file://src\components\MaterialDrawer.tsx#L15-L13)

**章节来源**
- [src\screens\EditorScreen.tsx:1-758](file://src\screens\EditorScreen.tsx#L1-L758)
- [src\components\ImageCanvas.tsx:1-91](file://src\components\ImageCanvas.tsx#L1-L91)
- [src\components\MaterialDrawer.tsx:1-136](file://src\components\MaterialDrawer.tsx#L1-L136)

### FinalizingScreen 最终化屏幕

FinalizingScreen 处理批量渲染和最终图像生成：

```mermaid
sequenceDiagram
participant Finalize as FinalizingScreen
participant Store as Zustand Store
participant API as API Utils
participant Backend as 后端服务
Finalize->>Store : 获取状态数据
Finalize->>API : 设置后端URL
alt 批量模式
Finalize->>API : renderAll(items)
API->>Backend : 批量渲染请求
Backend-->>API : 返回最终图像
API-->>Finalize : 最终图像数据
else 单张模式
Finalize->>API : finalizeV2(compositeImage)
API->>Backend : 最终化渲染请求
Backend-->>API : 返回最终图像
API-->>Finalize : 最终图像数据
end
Finalize->>Store : setFinalImage()
Finalize->>Store : setPhase('done')
```

**图表来源**
- [src\screens\FinalizingScreen.tsx:19-60](file://src\screens\FinalizingScreen.tsx#L19-L60)

**章节来源**
- [src\screens\FinalizingScreen.tsx:1-82](file://src\screens\FinalizingScreen.tsx#L1-L82)

### ResultSheet 结果展示

ResultSheet 提供最终图像的查看和保存功能：

**章节来源**
- [src\screens\ResultSheet.tsx:1-60](file://src\screens\ResultSheet.tsx#L1-L60)

## 依赖关系分析

### 组件间通信模式

系统采用多种通信模式确保组件间的松耦合：

```mermaid
graph LR
subgraph "单向数据流"
Store[Zustand Store] --> |状态订阅| Components[组件]
Components --> |动作触发| Store
end
subgraph "事件驱动"
MaterialDrawer --> |拖拽事件| EditorScreen
EditorScreen --> |点击事件| EditorScreen
UserInput[用户输入] --> |回调函数| Components
end
subgraph "函数式接口"
APIUtils[API工具] --> |Promise| Components
CanvasUtils[画布工具] --> |函数调用| EditorScreen
end
subgraph "本地存储"
LocalStorage[localStorage] --> |持久化| Store
Store --> |恢复状态| Components
end
```

**图表来源**
- [src\store.ts:63-176](file://src\store.ts#L63-L176)
- [src\utils\api.ts:1-200](file://src\utils\api.ts#L1-L200)

### 状态共享机制

状态管理采用集中式设计，通过 Zustand 提供响应式状态更新：

**章节来源**
- [src\store.ts:1-177](file://src\store.ts#L1-L177)
- [src\types.ts:14-88](file://src\types.ts#L14-L88)

## 性能考虑

### 渲染优化策略

1. **条件渲染**: 使用阶段状态精确控制组件渲染
2. **懒加载**: 图像和材质按需加载
3. **画布缓存**: 使用离屏画布减少重复计算
4. **防抖节流**: 处理高频事件如鼠标移动

### 内存管理

- 及时清理图像URL对象
- 合理使用 Refs 避免不必要的重渲染
- 控制画布尺寸与图像分辨率匹配

## 故障排除指南

### 常见问题及解决方案

1. **后端连接失败**
   - 检查后端服务状态
   - 验证 API 端点可达性
   - 确认跨域配置正确

2. **图像处理超时**
   - 检查网络连接稳定性
   - 验证图像格式支持
   - 监控后端模型加载状态

3. **材质拖拽异常**
   - 确认画布尺寸计算正确
   - 检查坐标转换逻辑
   - 验证蒙版命中检测

**章节来源**
- [src\screens\ProcessingScreen.tsx:80-85](file://src\screens\ProcessingScreen.tsx#L80-L85)
- [src\screens\FinalizingScreen.tsx:40-59](file://src\screens\FinalizingScreen.tsx#L40-L59)

## 结论

WallChanger 的组件架构展现了现代 React 应用的最佳实践：

1. **清晰的职责分离**: 屏幕组件专注于界面逻辑，可复用组件关注通用功能
2. **状态管理简洁**: Zustand 提供轻量级但强大的状态管理
3. **异步处理优雅**: 通过 Promise 和状态管理处理复杂的异步流程
4. **性能优化到位**: 画布渲染、内存管理和渲染优化策略完善

该架构为后续功能扩展提供了良好的基础，特别是在批量处理、多材质支持和高级编辑功能方面具有很大的扩展潜力。